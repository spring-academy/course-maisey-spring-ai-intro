# Run test classes in @Order sequence so OpenAiRecordingTest (@Order(1)) records and refreshes
# the classpath fixtures before OpenAiMockValidationTest (@Order(2)) replays them.
junit.jupiter.testclass.order.default=org.junit.jupiter.api.ClassOrderer$OrderAnnotation