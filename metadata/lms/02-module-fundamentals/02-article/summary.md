You now have the core mental model. Models are REST APIs, `ChatModel` is the portable contract over them, `ChatClient` is the fluent and batteries included API you reach for in everyday application code, and structured output turns model responses into type safe domain objects. In the next section you put all of this into practice in a hands-on lab, where you build the support assistant from an empty project up to a chat endpoint that returns a Java record.
